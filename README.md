# Orleans.Transactions.PostgreSql

Transactional State Provider for PostgreSQL, tested working on CockroachDB version 2.1.6

Before running the code, ensure SQL scripts are ran so that tables are created
